name: my_personal_bot

description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  speech_to_text: ^7.1.0
  http: ^1.4.0
  flutter_dotenv: ^5.0.2
  permission_handler: 12.0.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  # Removed .env from assets to avoid build errors on Vercel
  # assets:
  #   - assets/.env
